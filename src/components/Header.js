import React from 'react';

const Header = () => {
    return (
        <div>
            <Wrapper>
                <LogoWrapper>

                </LogoWrapper>
                <HomePageButton>

                </HomePageButton>

                <FollowingButton>

                </FollowingButton>

                <SearchWrapper>
                    <SearchBarWrapper>

                    </SearchBarWrapper>
                </SearchWrapper>

                <IconsWrapper>

                </IconsWrapper>
            </Wrapper>
        </div>
    );
};

export default Header;
